{
  "name": "may-hai-anh-export-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "astro dev",
    "cms:server": "decap-server",
    "dev:cms": "sh -c 'npx astro dev stop >/dev/null 2>&1 || true; if command -v lsof >/dev/null 2>&1; then pids=$(lsof -ti :8081 || true); [ -n \"$pids\" ] && kill $pids || true; fi; npm run dev -- --force; trap \"npx astro dev stop >/dev/null 2>&1 || true\" INT TERM EXIT; npm run cms:server'",
    "build": "astro check && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.9",
    "autoprefixer": "^10.5.3",
    "postcss": "^8.5.19",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.20",
    "decap-server": "^3.9.1"
  }
}
